# Map bundle name only: no directories, no suffix.
# Files are saved under saved_maps/<basename>/<basename>.*.
# Whitespace or empty string -> automatic name map_YYYYMMDD_HHMMSS (UTC).
string basename
---
bool success
string message
